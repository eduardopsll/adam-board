export interface Pictogram {
    audios: {[key: string]: string};
    categoryColor: string;
    categoryId: string;
    color: string;
    image: string;
    subcategoryNum: string;
    text: string;
}